// แก้ไข URL ด้านล่างให้ตรงกับ backend server ของคุณ
// ค่าเริ่มต้นจะใช้ localhost เพื่อให้สามารถรันได้ทันที
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3001';

export default API_URL;